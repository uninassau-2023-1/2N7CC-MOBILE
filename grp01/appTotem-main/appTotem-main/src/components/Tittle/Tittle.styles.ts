import { TextProps } from "react-native";

import styled from "styled-components/native";

export const TittleText = styled.Text<TextProps>`
  font-size: 20px;
  font-weight: bold;
  color: white;
`;
